# Core API stack
fastapi>=0.110,<1.0           # Web framework
uvicorn[standard]>=0.29,<1.0  # ASGI server with reload, httptools, uvloop

# Config & validation
pydantic[email]>=2.6,<3.0     # Pydantic + email validation (EmailStr)
python-dotenv>=1.0,<2.0       # Load env vars from .env

# SMS (Twilio)
twilio>=9.0,<10.0

# Background scheduler (reminders)
apscheduler>=3.10,<4.0

# Solver & data (keep only if you really use them)
ortools>=9.9,<10.0            # Google OR-Tools CP-SAT
pandas>=2.2,<3.0              # Optional: tabular utils for reports
numpy>=1.26,<3.0              # Required by pandas and sometimes by OR-Tools

# NOTE:
# - Do NOT pin Starlette/AnyIO/etc. FastAPI will pull compatible versions.
# - Keep this file small; it's easier to maintain and upgrade later.
